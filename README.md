# .github
SumatraPDF reader and SumatraPDF tabs keep documents open as files, not as a heavy workspace. SumatraPDF search finds a string in the current file so you can jump without paging the whole manual. Use the outline when it exists, and invert colors only for long text. Close unused files.
